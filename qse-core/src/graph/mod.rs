// Graph utilities — currently graph is built directly in scanner.
// This module is reserved for future graph transformations
// (e.g. filtering external nodes, building hybrid static+dynamic graph).
